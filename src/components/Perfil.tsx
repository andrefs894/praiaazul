// 3-step onboarding: profile type → location → max distance
// TODO (Week 5): build full UI

export default function Perfil() {
  return (
    <div className="p-4">
      <p className="text-gray-500">Perfil e onboarding — em construção</p>
    </div>
  )
}
